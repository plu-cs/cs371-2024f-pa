package diagonals;

public class Diagonals {

    public static boolean solve( DiagonalsBoard board ) {
        return solve(board, 0);
    }

    /**
     * Recursive method to solve the game.
     *
     * @param board the game board
     * @param cell the starting cell number
     * @return true if a solution is found.  The solution should be stored within
     * board.
     */
    private static boolean solve( DiagonalsBoard board, int cell ) {

        // TODO: Implement this method

        return false;
    }
}
